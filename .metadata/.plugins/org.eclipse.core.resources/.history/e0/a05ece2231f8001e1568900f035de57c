package modularização;

public class Delta {
public void calcularDelta(double a, double b, double c) {
	
	double resultado;
	resultado = Math.pow(b, 2) - (4*a*c);
	System.out.printf("Delta é: %.2f", resultado);
}}
